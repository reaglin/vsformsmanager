namespace VSFormsManager
{
    partial class frmAiSettings
    {
        /// <summary>
        /// Required designer variable.
        /// </summary>
        private System.ComponentModel.IContainer components = null;

        /// <summary>
        /// Clean up any resources being used.
        /// </summary>
        protected override void Dispose(bool disposing)
        {
            if (disposing && components != null)
                components.Dispose();

            base.Dispose(disposing);
        }

        /// <summary>
        /// The form UI is built entirely in code (frmAiSettings.cs → BuildUi()).
        /// This stub exists so Visual Studio recognises the file as a Form class
        /// and allows code-file editing. The visual designer will show a blank
        /// surface — open frmAiSettings.cs directly to modify the layout.
        /// </summary>
        private void InitializeComponent()
        {
            components = new System.ComponentModel.Container();
            AutoScaleMode = System.Windows.Forms.AutoScaleMode.Font;
        }
    }
}
